server:
  port: 12003
spring:
  application:
    name: spring-cloud-eureka

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceurl.defaultZone: http://${eureka.instance.hostname}:12002/eureka/